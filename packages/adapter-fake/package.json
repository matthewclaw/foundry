{
  "name": "@foundry/adapter-fake",
  "version": "0.1.0",
  "private": true,
  "description": "Foundry L2 scripted engine adapter (ADR-010): reads JSON scenario files and drives the ExecutionAdapter contract deterministically, at zero token cost. First-class product component, not test scaffolding.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "scenarios"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@foundry/adapter-api": "workspace:*",
    "@foundry/core": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.3"
  }
}
